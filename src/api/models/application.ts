export interface ExternalApplicationItem {
    name: string;
    icon: string;
    description: string;
    url: string;
}

export interface ApplicationInfo {
    app_title: string;
    welcome_text: string;
}
